import { StoryObj, Meta } from '@storybook/react';

import { Home } from '.';

export default {
  title: 'Pages/Index',
  component: Home,
  parameters: {
    layout: 'fullscreen',
  },
} as Meta;

export const Index: StoryObj = {};
